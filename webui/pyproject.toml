[project]
name = "bedrock-logging-webui"
version = "0.1.0"
description = "Web UI for Bedrock invocation logging analytics"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "boto3",
    "pandas",
]
